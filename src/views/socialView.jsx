import { observer } from "mobx-react-lite";

export const SocialView = observer(function SocialViewRender(props) {
    
    return (
        <div>
            <h1>Social</h1>
        </div>
    );
});